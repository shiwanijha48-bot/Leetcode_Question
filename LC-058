class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        return len(s.strip().split()[-1])

#  strip(): remove extra space from start and end
# split(): convert str into words
# [-1]: last word
#  len(): length

==================================================================
